<%= form_with(model: work_party) do |form| %>
  <% if work_party.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(work_party.errors.count, "error") %> prohibited this work_party from being saved:</h2>

      <ul>
        <% work_party.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <table class="table table-light table-borderless">
    <colgroup>
      <col style="width: 10%">
      <col style="width: auto">
    </colgroup>
    <tr>
      <td><%= form.label :name, style: "display: block" %></td>
      <td><%= form.text_field :name %></td>
    </tr>
    <tr>
      <td><%= form.label :work_type_id, class: 'form-label' %></td>
      <td><%= form.select :work_type_id, work_types, {}, { class: 'form-select' } %></td>
    </tr>
  </table>

  <div>
    <%= form.submit %>
  </div>
<% end %>
